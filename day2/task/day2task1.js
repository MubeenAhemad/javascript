let studentId = 101;
let studentName = "Tejas";
let studentMarks = 85;
let isPlaced = false;

let skills = ["Java", "HTML", "CSS"];

let address = {
    city: "Pune",
    pin: 411001
};

let company = null;

function checkType(val) {
    console.log(typeof(val))
}

checkType(studentId);      
checkType(studentName);    
checkType(studentMarks);   
checkType(isPlaced);       
checkType(skills);         
checkType(address);        
checkType(company);        